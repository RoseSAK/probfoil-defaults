#! /usr/bin/env python3


def main(n=50) :
    
    from random import random
    
    
    base(windok(day)).
    modes(windok(+)).
     
    base(pop(day)).
    modes(pop(+)).
     
    base(sunshine(day)).
    modes(sunshine(+)).
     
    base(surfing(day)).
    modes(surfing(+)).
    learn(surfing(day)).
    
    
    for i in range(0,n) :
    
        pop = random()
        
    
    


0.4987::pop(1).
0.3591::windok(1).
0.4534::sunshine(1).