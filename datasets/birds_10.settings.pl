mode(bird(+)).
base(bird(x)).

mode(blackbird(+)).
base(blackbird(x)).

%mode(sparrow(+)).
%base(sparrow(x)).

mode(thrush(+)).
base(thrush(x)).

%mode(robin(+)).
%base(robin(x)).

mode(eagle(+)).
base(eagle(x)).

mode(cat(+)).
base(cat(x)).

%mode(dog(+)).
%base(dog(x)).

%mode(rabbit(+)).
%base(rabbit(x)).

%mode(penguin(+)).
%base(penguin(x)).

%mode(dodo(+)).
%base(dodo(x)).

mode(ostrich(+)).
base(ostrich(x)).

%mode(kiwi(+)).
%base(kiwi(x)).

base(flies(x)).
learn(flies/1).
example_mode(closed).
