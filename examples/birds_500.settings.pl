learn(flies/1).
example_mode(closed).

learn(flies/1).

example_mode(closed).

mode(bird(+)).
base(bird(x)).

mode(penguin(+)).
base(penguin(x)).

mode(blackbird(+)).
base(blackbird(x)).

mode(sparrow(+)).
base(sparrow(x)).

mode(thrush(+)).
base(thrush(x)).

mode(robin(+)).
base(robin(x)).

mode(eagle(+)).
base(eagle(x)).
